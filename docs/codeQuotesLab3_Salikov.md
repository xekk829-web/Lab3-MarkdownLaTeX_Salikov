#### Код inline
я предпочитаю `while`, а не `for`.
#### Многострочный код
```python
isPreferWhile = input("Do you prefer use 'while'? [y/n]: ")
if isPreferWhile == "y":
    print("No")
elif isPreferWhile == "n";
    print("Yes")
else:
    print("idk")
```
Можно указывать язык: `python`, `js`, `bash`, `csharp` и т.д.

#### Цитаты
> Это цитата
>> Это цитата цитат
>>> Это цитата цитата цитат

***
#### Блоки кода
чтобы запушить коммит напиши `git push`.

**1000 - 7 на Python:**
```python
import time

a, b = 1000, 7
while a > 7:
    print(f"{a} - {b} = {a - b}")
    a -= b
    time.sleep(30)
```

***Hello World на Rust:***
```
void main() {
    println!("Hello World!")
}
```

> Красивое лучше, чем уродливое.
> Явное лучше, чем неявное. 
> Простое лучше, чем сложное.
> Читаемость имеет значение.
>> Код читают гораздо чаще, чем пишут.